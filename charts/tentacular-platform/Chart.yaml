apiVersion: v2
name: tentacular-platform
description: Umbrella Helm chart for the complete Tentacular platform - deploys MCP server, PostgreSQL, NATS, esm-sh proxy, namespace management, and network policies in a single install
type: application
version: 0.1.0
appVersion: "0.2.0"

home: https://github.com/randybias/tentacular-mcp
sources:
  - https://github.com/randybias/tentacular-mcp
  - https://github.com/randybias/tentacular

maintainers:
  - name: Randy Bias
    url: https://github.com/randybias

keywords:
  - tentacular
  - mcp
  - platform
  - exoskeleton
  - kubernetes

# Subchart dependencies
dependencies:
  - name: postgresql
    # Multi-arch images (linux/amd64 + linux/arm64) verified for this version
    # Latest in 16.x series; 18.x available but requires values schema migration
    version: "16.7.27"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: postgresql.enabled
  - name: nats
    # Multi-arch images (linux/amd64 + linux/arm64) - nats:2.11.10-alpine is multi-arch
    # Latest in 1.x series; 2.x available but requires values schema migration
    version: "1.3.16"
    repository: https://nats-io.github.io/k8s/helm/charts/
    condition: nats.enabled
  - name: cert-manager
    # Multi-arch images (linux/amd64 + linux/arm64) verified for this version
    version: "v1.20.0"
    repository: https://charts.jetstack.io
    condition: cert-manager.enabled
  - name: keycloakx
    # Codecentric Keycloakx (Quarkus-based modern Keycloak) - local fork
    # Patched to support namespaceOverride for umbrella chart deployments.
    # Uses official quay.io/keycloak/keycloak image.
    # Upstream: https://github.com/codecentric/helm-charts/tree/master/charts/keycloakx
    version: "7.1.9"
    repository: "file://../../charts/keycloakx"
    condition: keycloak.enabled
  - name: rustfs
    # Official RustFS chart - S3-compatible object storage, local fork
    # Patched to support namespaceOverride for umbrella chart deployments.
    # Upstream: https://charts.rustfs.com/
    version: "0.0.90"
    repository: "file://../../charts/rustfs"
    condition: rustfs.enabled
  - name: tentacular-mcp
    # Local reference for development; for releases use:
    # repository: oci://ghcr.io/randybias/tentacular-mcp/charts
    version: "0.1.0"
    repository: "file://../tentacular-mcp"
    condition: tentacular-mcp.enabled
