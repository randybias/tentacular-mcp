{{- if .Values.mtls.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "rustfs.fullname" . }}-server-tls
  namespace: {{ include "rustfs.namespace" . }}
spec:
  secretName: {{ include "rustfs.fullname" . }}-server-tls
  issuerRef:
    name: {{ include "rustfs.fullname" . }}-ca-issuer
    kind: Issuer
  commonName: {{ include "rustfs.fullname" . }}-cluster
  dnsNames:
    - "*.{{ include "rustfs.fullname" . }}-headless"
    - "*.{{ include "rustfs.fullname" . }}-headless.{{ include "rustfs.namespace" . }}.svc.cluster.local"
    - "{{ include "rustfs.fullname" . }}-svc.{{ include "rustfs.namespace" . }}.svc.cluster.local"
  {{- range .Values.ingress.hosts }}
    - {{ .host | quote }}
    - {{ printf "*.%s" .host | quote }}
  {{- end }}
  usages:
    - server auth
{{- end }}